package e2e

import (
	"fmt"

	. "github.com/onsi/ginkgo/v2"
)

var _ = Describe("GenerateFilteredSbom", func() {
	fmt.Println("do nothing (for now)")
	It("should create a filtered sbom", func() {
		fmt.Println("It is doing nothing")
	})
})
