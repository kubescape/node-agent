apiVersion: kubescape.io/v1
kind: RuntimeRuleAlertBinding
metadata:
  name: serviceref-test-binding
spec:
  namespaceSelector:
  podSelector:
    matchLabels:
      app: source-controller
  rules:
    - ruleName: "TEST internal egress not in profile"
